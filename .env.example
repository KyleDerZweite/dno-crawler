# DNO Crawler Environment Configuration
# Copy this to .env and fill in your values

# --- Backend Configuration ---

# Database
POSTGRES_USER=dno
POSTGRES_PASSWORD=CHANGE_ME_TO_A_STRONG_PASSWORD
POSTGRES_DB=dno_crawler
# DATABASE_URL=postgresql+asyncpg://dno:password@db:5432/dno_crawler
# USE_ALEMBIC_MIGRATIONS=true  # Set to true in production to use Alembic migrations

# Redis
REDIS_URL=redis://redis:6379/0

# --- Crawler Politeness (IMPORTANT for BFS crawling) ---
# Contact email shown in User-Agent when crawling external sites.
# REQUIRED in production (Zitadel auth enabled). In dev mode, falls back to user IP (comment out the line).
CONTACT_EMAIL=contact@your-company.de

# Zitadel Authentication (Backend)
# DISCLAIMER: If ZITADEL_DOMAIN is not set to a valid provider (e.g. left as auth.example.com), 
# the system will run in "No-Auth" mode with a mock Admin user. USE ONLY FOR DEVELOPMENT.
ZITADEL_DOMAIN=auth.example.com

# Rate Limiting & Storage
RATE_LIMIT_REQUESTS=100
RATE_LIMIT_PERIOD=60
STORAGE_PATH=/data

# Logging
LOG_LEVEL=INFO
ENVIRONMENT=development

# --- AI Configuration (Optional) ---
# Set this to automatically configure OpenRouter with the recommended model.
# If not set, you can configure AI providers via the Settings > AI Configuration page.
# OPENROUTER_KEY=sk-or-v1-your-key-here

# --- Frontend Configuration ---

# Backend API URL
VITE_API_URL=http://localhost:8000/api/v1

# Zitadel Authentication (Frontend)
# DISCLAIMER: If VITE_ZITADEL_AUTHORITY is not set to a valid provider (e.g. left as https://auth.example.com),
# the system will run in "No-Auth" mode. USE ONLY FOR DEVELOPMENT.
VITE_ZITADEL_AUTHORITY=https://auth.example.com
VITE_ZITADEL_CLIENT_ID=your-client-id-here
VITE_ZITADEL_REDIRECT_URI=http://localhost:5173/callback
VITE_ZITADEL_POST_LOGOUT_URI=http://localhost:5173

CORS_ORIGINS=["http://localhost:5173","http://localhost:3000"]

PANGOLIN_ENDPOINT=https://pangolin.example.com
NEWT_ID=your-newt-id-here
NEWT_SECRET=your-newt-secret-here